import { Meteor } from 'meteor/meteor';
import { Tempurature } from '/imports/api/Collections';

import  "/imports/api/Methos"
import  "/imports/api/Publisher"

Meteor.startup(async () => {
  // If the Links collection is empty, add some data.

});
